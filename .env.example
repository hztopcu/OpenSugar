# Ghost Mode (required for login)
APP_USER=your_username
APP_PASSWORD=your_secure_password

# Auth.js session encryption (required)
AUTH_SECRET=your_random_long_string

# Production: set to your app URL (e.g. https://yourapp.vercel.app). trustHost: true in auth.ts uses request host if unset.
# NEXTAUTH_URL=https://yourapp.vercel.app

# Postgres (Neon / Vercel Postgres)
POSTGRES_URL="postgres://..."
