# Copy to .env.local and fill in your values.
# Ghost Mode: single app credential (no signup).
APP_USER=your_username
APP_PASSWORD=your_secure_password

# Auth.js (generate: openssl rand -base64 32)
AUTH_SECRET=your_random_long_string

# Neon / Vercel Postgres (or any Postgres)
POSTGRES_URL="postgres://default:xxx@xxx.pooler.vercel-storage.com:5432/verceldb?sslmode=require"
